# Letta Open ADE Manual

This document is the durable operational handoff for the local ADE stack. For a quick architecture map and "where do I change X?" guide, start with `docs/codebase-map.md`.

## Goal

Run Letta, the first-party model router, the Agent Platform API, and the ADE frontend from this checkout with clear local configuration and verification paths.

## Current State

The current stack works end to end with:

- Letta server in Docker
- Postgres + pgvector in Docker
- `model_router` as the single OpenAI-compatible LLM front door
- llama-server, DGX/vLLM, and Ark as active router upstreams, with LM Studio and Unsloth Studio as optional standby sources
- Letta's built-in embedding backend for agent memory
- Two marimo notebook files that can also be executed as script-based smoke tests

Current development baseline defaults:

- System prompt: `CHAT_V20260418_PROMPT`
- Test embedding: `letta/letta-free`
- Comment Lab task shape: `classic`
- Label Lab schema: `label_entity_groups_v1`

## Directory Contents

- `compose.yaml`: Docker Compose stack
- `init.sql`: required database bootstrap
- `.env.example`: sanitized configuration template
- `.env`: local runtime configuration
- `pyproject.toml`: backend script/test dependency manifest for `uv`
- `uv.lock`: dependency lockfile generated by `uv`
- `README.md`: short run instructions
- `MANUAL.md`: this longer decision log and runbook
- `docs/codebase-map.md`: source map for backend services, frontend modules, configs, tests, and diagnostics
- `docs/development-conventions.md`: repo-specific structure and workflow locality conventions
- `notebooks/01_doubao_api_smoke.py`: direct Ark validation
- `notebooks/02_letta_e2e.py`: Letta end-to-end validation

Maintained verification entry points under `tests/`:

- `pytest` suites under `tests/test_*.py`
- `tests/checks/platform_api_e2e_check.py`
- `tests/checks/ade_mvp_smoke_e2e_check.py`

General-purpose operational scripts remain under `scripts/`. Evaluation/probe workflows with colocated config, inputs, docs, and outputs live under `evals/`.

## Core Decisions

### 1. Router-First LLM Access

Decision:
- Put all LLM upstream discovery, health, module visibility, and source-scoped routing behind `model_router`.

Why:
- Letta is easiest to keep stable when it sees one OpenAI-compatible provider.
- ADE modules need richer source diagnostics than blind OpenAI-compatible consumers can see.

Consequence:
- `config/model_router_sources.json` is the single source endpoint configuration file.
- `config/model_router_model_profiles.json` layers advisory model intelligence on discovered router models, including recommended `temperature`, `top_p`, `top_k`, and Agent Studio compatibility flags.
- Agent Studio sees router-backed Letta handles, while Comment Lab and Label Lab call the router directly.

### 2. Docker Compose For The Local Stack

Decision:
- Use Docker Compose with the pinned upstream Letta image and locally built first-party ADE services.

Why:
- The user explicitly preferred Compose.
- The official image is enough for this setup.
- Building `model_router`, `agent_platform_api`, and `ade_frontend` locally keeps the development loop direct.

Consequence:
- `letta_server` remains an upstream image.
- First-party ADE services build from this checkout.

### 3. Runtime Services

Decision:
- Keep only:
  - Letta server
  - Postgres + pgvector
  - Redis
  - model router
  - Agent Platform API
  - ADE frontend

Why:
- Nginx is not required for local validation.
- Each first-party service has one clear role.

Consequence:
- Letta API is exposed directly on `8283`.
- Letta ADE/UI is exposed directly on `8083`.
- Agent Platform API is exposed on `8284`.
- ADE frontend is exposed on `3000`.

### 4. Model Router Through Letta's OpenAI-Compatible Path

Decision:
- Configure Letta via:
  - `OPENAI_API_BASE=http://model_router:8290/v1`
  - `OPENAI_API_KEY=${MODEL_ROUTER_API_KEY}`

Why:
- Letta already supports OpenAI-compatible providers through this path.
- A first-party router lets ADE own provider naming, source health, module visibility, Ark allowlists, and future backend policy in one place.

Consequence:
- Letta discovers router-backed models as `openai-proxy/<source-id>::<provider-model-id>`.
- The preferred local handle is `openai-proxy/local_llama_server::gemma4` when llama-server is healthy and tool calling is compatible.

### 5. Router-Backed LLMs, Letta-Free Embeddings

Decision:
- Use router-backed chat models for agent LLMs.
- Use `letta/letta-free` for embeddings.

Why:
- The Ark key successfully supported:
  - `GET /models`
  - `POST /chat/completions`
  - OpenAI-style tool calling
- The tested embedding requests did not work with the same key on the tested compatibility path.

What failed:
- Alias-style embedding model name: `doubao-embedding`
- Tested explicit IDs returned by `/models`, such as:
  - `doubao-embedding-text-240715`
  - `doubao-embedding-large-text-240915`

Observed result:
- Ark returned `InvalidEndpointOrModel.NotFound` for those embedding requests with this key.

Consequence:
- Embedding proxying is intentionally out of scope while Letta's built-in embedding handle remains usable.
- If provider-specific embeddings are required later, treat that as a separate router capability.

### 6. Keep `init.sql`

Decision:
- Keep `init.sql` in the standalone bundle.

Why:
- It is still necessary for this database layout.
- It does all of the following:
  - connects to the target database
  - creates a dedicated schema named after the DB
  - sets the database `search_path`
  - creates the `vector` extension in that schema
  - drops the default `public` schema

Consequence:
- `init.sql` is a main file and should be preserved.
- Removing it changes the DB initialization behavior and would likely create avoidable startup/runtime issues.

### 7. Use `uv` Instead Of `pip`

Decision:
- Use `uv` as the dependency manager for the notebooks.

Why:
- The user explicitly requested it.
- The main Letta repo already uses `uv`.
- `uv sync` + `uv.lock` gives a cleaner portable workflow than `pip install -r requirements.txt`.

Consequence:
- `pyproject.toml` and `uv.lock` are the source of truth for Python dependencies.
- The old `requirements.txt` was removed to avoid two competing workflows.

### 8. Add `.dockerignore`, But Treat It As Future-Proofing

Decision:
- Add a minimal `.dockerignore`.

Why:
- It keeps CI Docker build context small for Agent Platform API image publishing.
- It avoids shipping local artifacts (for example `data/`, `.tmp/`, diagnostics outputs) into image builds.

Consequence:
- `.dockerignore` is part of active CI/CD performance and reproducibility.
- It reduces build time and accidental context bloat.

### 9. Local Build Deployment Model

Decision:
- Use pinned upstream Letta image for `letta_server`.
- Build first-party ADE services from this checkout for now.

Why:
- Keeps Letta maintenance burden low (no custom forked server image lifecycle).
- Keeps the active development loop simple while the router/API surfaces are still moving.

Consequence:
- `LETTA_SERVER_IMAGE` still controls the upstream Letta image.
- `agent_platform_api`, `model_router`, and `ade_frontend` build locally through Compose.

## Verified Findings

These were actually tested, not assumed:

### Ark / Doubao Directly

Verified:
- `GET /models` works with the provided key.
- `POST /chat/completions` works with `doubao-seed-1-8-251228`.
- A tool-calling request produced an OpenAI-style `tool_calls` payload.

Direct smoke output confirmed:
- model discovery
- exact chat response
- exact function name in returned `tool_calls`

### Letta End To End

Verified:
- Letta container starts cleanly when pointed at `model_router`.
- Letta lists router model handles such as:
  - `openai-proxy/local_llama_server::gemma4`
- Letta lists the embedding handle:
  - `letta/letta-free`
- Letta can create an agent with those handles.
- Letta can run a message through a router-backed agent when the selected upstream supports the needed chat/tool behavior.

## 2026-04 Debug Journey Notes

### Letta Startup Delay Root Cause (NLTK)

Observed:
- In restricted/no-egress environments, startup could stall at `Checking NLTK data availability...` while watchdog logs continued.

Root cause:
- Letta startup calls `nltk.download("punkt_tab")` during lifespan before readiness is achieved.
- Even with local NLTK data present, that download call may still attempt network access.

Implemented fix in this bundle:
- Pre-seeded local NLTK cache (`data/nltk_data`) via `scripts/seed_nltk_data.sh`.
- Mounted local cache into the Letta container.
- Added runtime patch at `docker/sitecustomize.py` to short-circuit `punkt_tab` download when local data exists, with strict-local mode and timeout fallback.

Validation:
- Internal no-egress simulation reached healthy state.
- In-network curl health probe succeeded.

### agent_platform_api Cold-Start Delay Root Cause

Observed:
- `agent_platform_api` startup logs showed runtime env creation and package download (for example `Downloading pydantic-core`), causing slow first response.

Root cause:
- `agent_platform_api` previously ran `uv run ...` directly in container startup, which performs dependency sync at runtime.

Implemented fix in this bundle:
- Added `docker/agent_platform_api.Dockerfile`.
- Moved dependency installation to image build (`uv sync --frozen --no-dev --no-install-project`).
- Kept Uvicorn runtime entrypoint from `/opt/venv/bin/uvicorn` so local Compose builds do not sync dependencies at container startup.

Expected result:
- After build, `agent_platform_api` boots directly to Uvicorn without startup-time dependency download.

## Runtime Details That Matter

### Unified Model Router

Important:
- `model_router` now uses `config/model_router_sources.json` as the single source endpoint config for Agent Studio, Comment Lab, Label Lab, and future ADE modules.
- `config/model_router_model_profiles.json` stores model-level defaults and compatibility notes discovered from model cards, provider docs, and local smoke tests.
- Letta is pointed at the router through `OPENAI_API_BASE=http://model_router:8290/v1` in Compose, so it only needs one OpenAI-compatible provider.
- `agent_platform_api` reads `/v1/router/model-catalog` and calls the router for stateless Comment Lab and Label Lab generation. The router is mandatory for normal ADE model access.

Notes:
- Each source entry declares the upstream base URL, adapter, module visibility, and auth lookup fields.
- Auth resolution order is `/run/secrets/<api_key_secret>` first, then the env var named by `api_key_env`.
- Router model ids use `<source-id>::<provider-model-id>`, for example `local_llama_server::gemma4`.
- Letta sees router models as `openai-proxy/<source-id>::<provider-model-id>`, for example `openai-proxy/local_llama_server::gemma4`.
- Agent Studio only surfaces router models whose Letta handles are actually registered in Letta.
- Comment Lab uses source-scoped model keys in the form `<source-id>::<provider-model-id>` so duplicate model names remain distinct across providers.
- The local port split is intentional:
  - `8081` is the active llama-server endpoint for local Comment Lab and Label Lab work.
  - `100.64.35.71:8000` is the DGX Spark vLLM endpoint for larger local models and is initially visible to Comment Lab and Label Lab only.
  - `2234` remains optional Unsloth Studio standby.
  - `1234` remains optional LM Studio standby plus Letta bootstrap compatibility.
- Model profiles are advisory. The router fills omitted `temperature`, `top_p`, and supported `top_k` values from the profile, but explicit caller values always win.
- llama-server runtime settings remain host-side operations. Loaded GGUF, context sizing, GPU offload, and reasoning mode are expected to be adjusted in the launch command on the machine running llama-server, not through ADE.
- Unsloth Studio and LM Studio may be offline or have no loaded model for long periods. In that state they should either be disabled in `config/model_router_sources.json` or appear only as `unreachable`/`empty` in diagnostics without blocking normal llama-server-first use.
- Ark model visibility is filtered through the checked-in allowlist report at `agent_platform_api/catalog_data/ark_chat_probe_report.json`, which is regenerated with `uv run python evals/provider_model_probe/run.py --source-id ark --mode chat-probe --write`.
- Ark is currently enabled for chat/comment only. Label Lab is centered on llama-server JSON Schema output until a provider is explicitly enabled and verified for label schemas.

### Label Lab And Schema Center

Important:
- Label Lab is stateless like Comment Lab, but it produces structured grouped entity extraction instead of prose.
- Prompts define labeling criteria. Schemas define the JSON output contract.
- The default label schema is stored at `schemas/label/label_entity_groups_v1.json` and is surfaced through the Label Schema Center.

Notes:
- Schema Center v1 is label-only. It provides create, edit, archive, restore, and purge operations under `/api/v1/platform/schema-center/label-schemas`.
- The API container mounts `./schemas:/app/schemas`, so schema edits persist to the host filesystem.
- Persona records are stored in the tracked SQLite DB at `data/personas/personas.sqlite3` and seeded from `agent_platform_api/seed_data/personas.jsonl`; system prompts remain file-backed. SQLite sidecars stay ignored. Use `uv run python scripts/persona_library.py --help` for JSONL/Markdown exchange.
- `POST /api/v1/labeling/generate` requires a selected `model_key`, `prompt_key`, and `schema_key`.
- For the `llama_cpp_server` adapter, Label Lab sends `response_format={"type":"json_schema","json_schema":...}` to `/v1/chat/completions`, matching the notebook-proven llama-server pattern.
- Server-side validation still verifies the parsed shape, trims duplicates, and requires every extracted string to exactly match a substring in the input article.
- Duplicate values are removed after trimming, but missing or non-exact substrings still fail validation.

### Ark Usability Allowlist

Important:
- Ark's raw `/models` response is broader than the set of models that actually answer a minimal text `/chat/completions` request.
- ADE therefore uses a persisted allowlist generated by the probe script instead of trusting raw discovery for UI visibility.

Current seeded usable set:
- `doubao-1-5-lite-32k-250115`
- `doubao-seed-1-6-250615`
- `doubao-seed-1-6-vision-250815`
- `doubao-seed-1-6-251015`
- `glm-4-7-251222`
- `doubao-seed-1-8-251228`
- `doubao-seed-2-0-lite-260215`
- `doubao-seed-2-0-mini-260215`
- `doubao-seed-2-0-pro-260215`

### Model Handle Naming

Important:
- In Letta, the Doubao model handle is not `openai/doubao-seed-1-8-251228`.
- It is `openai-proxy/doubao-seed-1-8-251228`.

Reason:
- Letta uses `openai-proxy` for non-OpenAI providers routed through the OpenAI-compatible provider implementation.

### Full Model ID Matters

Important:
- Use the actual Ark model ID, not the display name.

Working:
- `doubao-seed-1-8-251228`

Do not assume:
- `doubao-seed-1-8` alone will behave the same way in Letta's discovered model list.

### Letta Model Routes Redirect

Observed:
- Letta's model listing route redirects to a trailing-slash URL.

Example:
- `GET /v1/models` redirected to `GET /v1/models/`

Consequence:
- The e2e notebook was updated to use `follow_redirects=True` and `/v1/models/`.

### Marimo Notebook Files Also Run As Scripts

Decision:
- The notebook files support both:
  - normal marimo UI usage
  - script-style smoke execution

Mechanism:
- Set `MARIMO_SMOKE_ONLY=1` and run the notebook with Python.

Why:
- This makes smoke tests automatable without opening the notebook UI.

## Fresh Start Runbook

If you move this directory somewhere else and want a clean start:

1. Copy the entire directory.
2. Keep or recreate `.env`.
3. If you want a truly fresh DB, remove `data/pgdata`.
4. Run:

```powershell
uv sync
docker compose up -d
```

5. Run the smoke notebooks:

```powershell
$env:MARIMO_SMOKE_ONLY=\"1\"
uv run python notebooks\\01_doubao_api_smoke.py
uv run python notebooks\\02_letta_e2e.py
Remove-Item Env:MARIMO_SMOKE_ONLY
```

6. Run development verification:

```powershell
$env:PYTHONPATH="."
uv run python -m pytest
uv run python tests/checks/platform_api_e2e_check.py
uv run python tests/checks/ade_mvp_smoke_e2e_check.py
```

7. If you launch checks from Test Center, inspect transient orchestrator logs under:

- `tests/outputs/platform_orchestrator/`

8. If you want the notebook UI instead of script mode:

```powershell
uv run marimo run notebooks\\01_doubao_api_smoke.py --headless
uv run marimo run notebooks\\02_letta_e2e.py --headless
```

## What To Change If You Swap Models

If you change the default Agent Studio model later:

Update both:
- `config/model_router_sources.json` `enabled_for` module tags for the desired upstream/model
- `config/model_router_model_profiles.json` if the selected model has recommended sampling defaults or needs an Agent Studio compatibility flag
- `LETTA_MODEL_HANDLE`

Rule:
- `LETTA_MODEL_HANDLE` should match Letta's discovered handle format:
  - `openai-proxy/<source-id>::<provider-model-id>`

Recommended procedure:
1. Confirm the model appears in `GET /v1/models` on `model_router`.
2. Update `LETTA_MODEL_HANDLE` to match.
3. Restart Letta so it rediscovers router models.
4. Run the direct router smoke first.
5. Then run the Letta e2e notebook.

If you change ADE provider discovery later:

Update:
- `config/model_router_sources.json`
- `config/model_router_model_profiles.json` if the new provider/model needs model-specific sampling or capability notes
- no second Agent Platform source file is needed; `agent_platform_api` reads router diagnostics instead of traversing providers directly
- any referenced source auth env vars such as `LLAMA_SERVER_API_KEY`, `UNSLOTH_API_KEY`, or `OPENAI_API_KEY`

## If You Want Pure Doubao Embeddings Later

This is the biggest unresolved area.

What is already known:
- The tested Ark compatibility path worked for chat and tool calling.
- The tested embedding requests did not work with the same key.

Possible next steps:
- Check whether Ark embeddings require different model IDs than the compatibility `/models` response implies.
- Check whether the account/key lacks access to the embedding models.
- Check whether Volcengine expects endpoint IDs or another naming layer for embeddings.
- If embeddings become available, update:
  - `.env`
  - the e2e notebook default embedding choice
  - the documentation

## If You Need External Exposure Later

Current state:
- No reverse proxy
- No TLS
- No extra auth layer beyond what Letta itself does

If the stack is exposed beyond local development:
- Add a reverse proxy
- Add TLS
- Decide on auth and network boundaries explicitly

Nginx was intentionally omitted from this bundle because it is not needed for local validation.

## If You Add More Services Later

This bundle is meant to be extended.

Good candidates:
- an app service that calls Letta
- a reverse proxy
- a monitoring service
- a worker service

Notes:
- `.dockerignore` is already in place for future build contexts.
- Keep the Letta and DB pieces isolated and stable before layering more services on top.

## Commands That Were Proven To Work

From this directory:

```powershell
uv sync
docker compose up -d
$env:MARIMO_SMOKE_ONLY=\"1\"
uv run python notebooks\\01_doubao_api_smoke.py
uv run python notebooks\\02_letta_e2e.py
Remove-Item Env:MARIMO_SMOKE_ONLY
docker compose down
```

## Short Answers To Likely Future Questions

### Is `init.sql` still necessary?

Yes.

### Do we need `.dockerignore` right now?

Not for current behavior, because there is no Docker build in this bundle. It was added as future-proofing.

### Why not use the repo Dockerfile?

Because the official Letta image already works, and the goal was a portable standalone folder.

### Why not use Doubao embeddings?

Because the tested Ark key/path did not produce a working embedding request, while Letta's built-in embedding handle did.

### Why keep both `.env.example` and `.env`?

`.env.example` is the safe template. `.env` is the local runnable config.

## Useful Upstream References

- Letta Docker guide: https://docs.letta.com/guides/docker/
- Letta OpenAI-compatible provider guide: https://docs.letta.com/guides/server/providers/openai-proxy/
- Volcengine Ark OpenAI compatibility: https://www.volcengine.com/docs/82379/1330626?lang=zh
- Volcengine Ark Chat API: https://www.volcengine.com/docs/82379/1494384?lang=zh
- Volcengine Ark Function Calling: https://www.volcengine.com/docs/82379/1262342?lang=zh

